{
  "hooks": {
    "PreCommit": [
      {
        "command": "npm run lint && npm run build",
        "description": "Lint and build before committing"
      }
    ],
    "SessionStart": [
      {
        "command": "test -d node_modules || npm install; npm run build -s 2>/dev/null && echo 'Build OK' || echo 'WARNING: build is broken'",
        "description": "Verify environment ready"
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(npm run *)",
      "Bash(node scripts/*)",
      "Bash(npx vite *)",
      "Bash(ls dist/*)",
      "Bash(ls src/*)"
    ]
  }
}
